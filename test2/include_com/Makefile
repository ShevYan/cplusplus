# SlickEdit generated file.  Do not edit this file except in designated areas.

# Make command to use for dependencies
MAKE=gmake
RM=rm
MKDIR=mkdir

# -----Begin user-editable area-----

# -----End user-editable area-----

# If no configuration is specified, "Debug" will be used
ifndef CFG
CFG=Debug
endif

#
# Configuration: Debug
#
ifeq "$(CFG)" "Debug"
OUTDIR=Debug
OUTFILE=$(OUTDIR)/testRPC
CFG_INC=-I../include_com 
CFG_LIB=-lpthread -lRPCDLL -lCdetect -lTcpIPCommon -lcomutility -lrt \
	-ldl -lfuse -lnixscmd -lFimas -lLog -L/home/lfm/lib 
CFG_OBJ=
COMMON_OBJ=$(OUTDIR)/pipetransfer.o $(OUTDIR)/testRPC.o 
OBJ=$(COMMON_OBJ) $(CFG_OBJ)
ALL_OBJ=$(OUTDIR)/pipetransfer.o $(OUTDIR)/testRPC.o -lpthread \
	-lRPCDLL -lCdetect -lTcpIPCommon -lcomutility -lrt -ldl -lfuse \
	-lnixscmd -lFimas -lLog -L/home/lfm/lib 

COMPILE=g++ -c  "-D_POSIX_" "-DFI_LINUX" -g -pg -o "$(OUTDIR)/$(*F).o" $(CFG_INC) $<
LINK=g++  -g -pg -o "$(OUTFILE)" $(ALL_OBJ)

# Pattern rules
$(OUTDIR)/%.o : %.cpp
	$(COMPILE)

# Build rules
all: $(OUTFILE)

$(OUTFILE): $(OUTDIR)  $(OBJ)
	$(LINK)
	-cp ./Debug/testRPC ~/lib

$(OUTDIR):
	$(MKDIR) -p "$(OUTDIR)"

# Rebuild this project
rebuild: cleanall all

# Clean this project
clean:
	$(RM) -f $(OUTFILE)
	$(RM) -f $(OBJ)

# Clean this project and all dependencies
cleanall: clean
endif

#
# Configuration: Release
#
ifeq "$(CFG)" "Release"
OUTDIR=Release
OUTFILE=$(OUTDIR)/testRPC
CFG_INC=-I../include_com 
CFG_LIB=-lpthread -lRPCDLL -lCdetect -lTcpIPCommon -lcomutility -lrt \
	-ldl -lfuse -lnixscmd -lFimas -lLog -L/home/lfm/lib 
CFG_OBJ=
COMMON_OBJ=$(OUTDIR)/pipetransfer.o $(OUTDIR)/testRPC.o 
OBJ=$(COMMON_OBJ) $(CFG_OBJ)
ALL_OBJ=$(OUTDIR)/pipetransfer.o $(OUTDIR)/testRPC.o -lpthread \
	-lRPCDLL -lCdetect -lTcpIPCommon -lcomutility -lrt -ldl -lfuse \
	-lnixscmd -lFimas -lLog -L/home/lfm/lib 

COMPILE=g++ -c  "-D_POSIX_" "-DFI_LINUX" -pg -o "$(OUTDIR)/$(*F).o" $(CFG_INC) $<
LINK=g++  -pg -o "$(OUTFILE)" $(ALL_OBJ)

# Pattern rules
$(OUTDIR)/%.o : %.cpp
	$(COMPILE)

# Build rules
all: $(OUTFILE)

$(OUTFILE): $(OUTDIR)  $(OBJ)
	$(LINK)
	-cp ./Debug/testRPC ~/lib

$(OUTDIR):
	$(MKDIR) -p "$(OUTDIR)"

# Rebuild this project
rebuild: cleanall all

# Clean this project
clean:
	$(RM) -f $(OUTFILE)
	$(RM) -f $(OBJ)

# Clean this project and all dependencies
cleanall: clean
endif
