#pragma once
#include "libcfg.h"
#include "difstypes.h"
#include "OSThread.h"
#include "FiContext.h"

/**
 * 1. ????id??????????????
 * 2. ???????"/FiConfig/FiDict"???????????????
 * <FiConfig>
 *         <lang>zh_cn.xml</lang>
 * </FiConfig>
 * 
 * 3. ?????utf8???
 * 4. ???????????????dict???
 */
class FiDict
{
private:
	static CCritSec s_lock;
	static FiDict*	volatile s_pInstance;
	static string 	s_strDictPath;
	CFiContext* 	m_pCxt;

	FiDict();
public:
	static FiDict* GetInstance();

	string GetText(const char *strId);

	~FiDict(){ }
};
